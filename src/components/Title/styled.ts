import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxTitle: {
    alignContent: "center",
    justifyContent: "center",
    padding: 10,
  },

  textTitle: {
    color: "#F9629F",
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
  },
});
